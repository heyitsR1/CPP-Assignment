#include <iostream> 
#include <string>
#include <algorithm>
using namespace std;

string longest_word (string line);

int main () {
    string in;
    cout << "Enter the string: "; 
    getline(cin, in);
    cout >> "Longest word is " << longest_word(in) << endl;
    return 0;
}

string longest_word (string str){ 
    for (

}